source "https://rubygems.org"

# Specify your gem's dependencies in active_record_custom_preloader.gemspec
gemspec

if ENV['RAILS_VERSION']
  gem 'activerecord', ENV['RAILS_VERSION']
else
  gem 'activerecord'
end

gem 'rake'
